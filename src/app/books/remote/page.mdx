import WritingPage from '@/components/WritingPage'
import { buildBookMetadata } from '@/lib/metadata'

export default ({ children }) => <WritingPage meta={meta} children={children} />

export const meta = {
    id: 'remote',
    title: 'Remote',
    name: 'Remote',
    author: 'Jason Fried, David Heinemeier Hansson',
    foregroundColor: '#F90000',
    backgroundColor: '#FFF',
    date: '2020-03-21T20:44:00+0300'
}

export const metadata = buildBookMetadata(meta)
